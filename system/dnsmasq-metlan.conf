# MetLAN Pi router - DHCP + DNS for eth0 (LAN/testbench).
# Install: sudo cp system/dnsmasq-metlan.conf /etc/dnsmasq.d/metlan.conf && sudo systemctl restart dnsmasq
# See system/README.md before enabling this on anything but the isolated testbench.

interface=eth0
# bind-dynamic (not bind-interfaces): survives eth0 not existing yet at
# dnsmasq startup - confirmed 2026-09-23 that bind-interfaces makes
# dnsmasq hard-fail ("unknown interface eth0") on a boot-order race, with
# no automatic recovery, silently killing DNS for the whole testbench LAN.
bind-dynamic

dhcp-range=10.42.0.10,10.42.0.200,12h
dhcp-option=option:router,10.42.0.1
dhcp-option=option:dns-server,10.42.0.1

no-resolv
server=1.1.1.1
server=8.8.8.8
